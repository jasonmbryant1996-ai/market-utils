# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Model files — DO NOT commit these to git
# They are large binaries that should be uploaded separately
# See model/README.md for instructions
model/*.pth
model/*.pkl

# NOTE: state/last_signal.json is intentionally NOT ignored — it is
# committed directly to the repo by live_monitor.py (git_commit_state())
# so paper-trade state survives job restarts/timeouts. See README.md.

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

#
.private